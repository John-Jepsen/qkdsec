name: Publish to PyPI

# Publishes on tag push (e.g., v0.1.0). Uses PyPI Trusted Publishing (OIDC) —
# no API tokens stored. Before first use, configure the trusted publisher at:
#   https://pypi.org/manage/account/publishing/
# with: project=qkdsec, owner=John-Jepsen, repo=qkdsec, workflow=publish.yml.

on:
  push:
    tags:
      - "v*"

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install package with all extras
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[all,test]"

      - name: Run tests
        run: pytest -v

  build:
    name: Build distributions
    runs-on: ubuntu-latest
    needs: test

    steps:
      - uses: actions/checkout@v4
        with:
          # setuptools-scm derives the version from git tags
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Build sdist and wheel
        run: python -m build

      - name: Verify distributions
        run: twine check dist/*

      - name: Verify tag matches package version
        run: |
          TAG="${GITHUB_REF#refs/tags/v}"
          PKG_VERSION=$(python -c "import pathlib; print(next(pathlib.Path('dist').glob('qkdsec-*.whl')).name.split('-')[1])")
          if [ "$TAG" != "$PKG_VERSION" ]; then
            echo "Tag $TAG does not match built package version $PKG_VERSION"
            exit 1
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  publish:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: pypi
      url: https://pypi.org/p/qkdsec
    permissions:
      id-token: write   # Required for PyPI trusted publishing (OIDC)

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
